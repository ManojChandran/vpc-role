---
# create subnets
- name: Create subnet
  ec2_vpc_subnet:
    vpc_id:           "{{ vpc_id }}"
    region:           "{{ aws_region }}"
    cidr:             "{{ item.aws_az_cidr }}"
    az:               "{{ item.aws_az }}"
    state:            "present"
  register: "pvt_subnet_az"
  with_items: "{{ vpc_network_map_pvt }}"

- debug:
    msg: "register id {{ item }} "
  with_items: "{{ pvt_subnet_az }}"
